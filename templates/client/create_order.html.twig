{% extends 'base.html.twig' %}

{% block body %}
    <div style="margin-top: 100px">
        {{ form_start(createOrderForm) }}
            {{ form_row(createOrderForm.orderName) }}
            {{ form_row(createOrderForm.description) }}
            {{ form_row(createOrderForm.cost) }}
            {{ form_row(createOrderForm.address) }}

            <button type="submit">Create</button>

        {{ form_end(createOrderForm) }}
    </div>
{% endblock %}